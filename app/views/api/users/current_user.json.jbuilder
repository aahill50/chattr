 json.extract! current_user, :id, :fullname, :_username, :email,
                             :post_count, :follower_count, :following_count,
                             :bio, :join_date, :avatar_url
